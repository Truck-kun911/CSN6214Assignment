{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Rebuild server",
			"command": "${workspaceFolder}/buildServer.sh"
		},
		{
			"type": "shell",
			"label": "Rebuild client",
			"command": "${workspaceFolder}/buildClient.sh"
		}
	]
}